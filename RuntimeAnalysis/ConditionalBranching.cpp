#include<iostream>
using namespace std;

/*
The Runtime of an if-else statement (conditional) is bound by the runtime of the conditional test + 
    larger of the runtimes of the executions

Runtime Calculation:
    O(condition) + max(O(s1), O(s2))
*/


int main(){


    if (true)
    {
        //Block 1
    } 
    else
    {
        //Block 2
    }
    
    


    return 0;
}